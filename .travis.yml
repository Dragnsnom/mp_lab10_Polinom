language: cpp
os: linux
dist: Bionic
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../mp_lab10_Arithmetic
  - make -j
  - ./test/MP10_TESTS
notifications:
  email: false
